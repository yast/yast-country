/**
 * File:
 *   cfg_keyboard.scr
 * Summary:
 *   SCR Agent for reading/writing /etc/sysconfig/keyboard
 *   using the sysconfig-agent
 *
 * $Id$
 *
 * Read/Sets the values defined in <tt>/etc/sysconfig/keyboard</tt>
 * in an easy manner.
 */
.sysconfig.keyboard

`ag_ini(
  `SysConfigFile("/etc/sysconfig/keyboard")
)
